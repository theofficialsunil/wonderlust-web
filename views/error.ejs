<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Error <%= status %></title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      background-color: #f8f9fa;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      text-align: center;
      padding: 20px;
      font-family: Arial, sans-serif;
    }
    .error-card {
      max-width: 500px;
      width: 100%;
      border-radius: 12px;
      padding: 30px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      background: white;
    }
    .error-emoji {
      font-size: 4rem;
      margin-bottom: 15px;
    }
    .btn-custom {
      background: #ff385c;
      border: none;
    }
    .btn-custom:hover {
      background: #e0314b;
    }
  </style>
</head>

<body>
  <div class="error-card">
    <% if (status == 404 || status == "404") { %>
      <div class="error-emoji">🚫</div>
      <h2 class="fw-bold">404 - Page Not Found</h2>
      <p class="text-muted">The page you're trying to access doesn't exist.</p>
    <% } else if (status == 500 || status == "500") { %>
      <div class="error-emoji">⚠️</div>
      <h2 class="fw-bold">500 - Internal Server Error</h2>
      <p class="text-muted">Something went wrong on our server.</p>
    <% } else { %>
      <div class="error-emoji">⚠️</div>
      <h2 class="fw-bold"><%= status %> Error</h2>
      <p class="text-muted"><%= message %></p>
    <% } %>

    <a href="/listings" class="btn btn-custom mt-3 px-4">
      🏠 Back to Listings
    </a>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
